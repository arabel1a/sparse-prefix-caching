overwrite: True

defaults:
  - config
  - _self_

output_dir: outputs/toy
run_name: toytest
data:
  max_seq_len: 16384
  max_rows: 100

benchmark_baselines:
  seq_lens: [128, 256, 512, 1024]

benchmark_e2e:
  kv_budget_gb: 0.1
  gdn_budget_gb: 0.1

strategies:
  - no_cache
  - hist_frozen
