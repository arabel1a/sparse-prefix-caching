overwrite: True

defaults:
  - config
  - _self_

run_name: toy
data:
  max_rows: 100
  max_requests: 100

benchmark_baselines:
  seq_lens: [128, 256, 512, 1024]

benchmark_e2e:
  kv_budget_gb: 0.5
  gdn_budget_gb: 0.5
